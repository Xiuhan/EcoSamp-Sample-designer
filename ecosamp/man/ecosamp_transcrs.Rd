% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ecosamp_transcrs.R
\name{ecosamp_transcrs}
\alias{ecosamp_transcrs}
\title{EcoSamp CRS transformation function}
\usage{
ecosamp_transcrs(map, sd_crs, map_res = 5, ref_map = NULL)
}
\arguments{
\item{map}{RasterLayer, SpatRaster or SF object, map produced/specified in the ecosamp() function,
which needs to be transformed in CRS and/or object class.}

\item{sd_crs}{String, the target UTM CRS code produced/specified in the ecosamp() function.
generated by prior runs in the sample generation loop of the ecosamp() function.}

\item{map_res}{Numeric, the resolution (in metres) of output maps, specified in the ecosamp() function,
5(m) by default}

\item{ref_map}{RasterLayer, a map derived from map_habitat produced/specified in the ecosamp() function,
for cropping map_slope to the same area as map_habitat.}
}
\value{
A transformed version of target map, in RasterLayer or SpatVector class.
}
\description{
This function is a sub-function of ecosamp() function, which doesn't work on its own.
It loads the input target map, CRS, resolution and reference map (if provided),
generated/specified in the main function.
The function then transforms the CRS of the target map, and converts it based on its class:
\enumerate{
\item RasterLayer object: transforms map CRS into sd_crs, crop it to the same area as habitat map, if not the same.
\item SF object: converts map into a SpatVector object and transforms map CRS into sd_crs
(this is for calculating distance to roads/waters).
\item SpatRaster object: crop map to the same area as habitat map, if not the same,
and transforms map CRS into sd_crs.
}
}
\details{
The function then outputs the transformed version of map.
}
